spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

spring.docker.compose.enabled: false

# Un-Comment If you want tests to work in Docker:

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/config-and-feature-flag-service
#    username: postgres
#    password: localdocker
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#
#spring.docker.compose.enabled: false
